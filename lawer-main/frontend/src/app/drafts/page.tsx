"use client";
import { useCallback,useEffect,useState } from "react";
import Link from "next/link";
import { ArrowLeft,Clock,FileText,Loader2,Plus } from "lucide-react";
import Navbar from "@/components/layout/Navbar";import Footer from "@/components/layout/Footer";import { apiRequest,ApiClientError } from "@/lib/apiClient";
type Contract={id:number;serialNumber:string;title:string;status:string;sourceChannel:string;variantKey:string;updatedAt:string;templateSlug:string;templateNameAr:string};
export default function DraftsPage(){const [items,setItems]=useState<Contract[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{const data=await apiRequest<Contract[]>("/api/v1/contracts/my");setItems(data.filter(c=>["draft","revision_requested"].includes(c.status)));}catch(e){if(e instanceof ApiClientError&&e.status===401)setError("سجل الدخول لعرض المسودات المحفوظة على حسابك.");else setError(e instanceof Error?e.message:"تعذر تحميل المسودات");}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);return <div className="flex min-h-screen flex-col bg-[#f8fafc]"><Navbar/><main className="mx-auto w-full max-w-6xl flex-1 px-4 py-10 sm:px-6"><header className="mb-8 flex flex-col justify-between gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-center"><div><span className="rounded-full border border-blue-200 bg-blue-50 px-3 py-1 text-xs font-black text-blue-700">مسودات الحساب</span><h1 className="mt-2 text-3xl font-black text-slate-950">العقود غير المكتملة</h1><p className="mt-1 text-sm text-slate-600">المسودات التي حفظتها في حسابك ويمكنك الرجوع إليها واستكمالها لاحقًا.</p></div><Link href="/create-contract?mode=self_service" className="inline-flex items-center gap-2 rounded-xl bg-blue-700 px-5 py-3 text-xs font-black text-white"><Plus className="h-4 w-4"/>عقد جديد</Link></header>{loading?<div className="rounded-2xl border bg-white p-12 text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-blue-700"/></div>:error?<div className="rounded-2xl border border-amber-200 bg-amber-50 p-8 text-center"><p className="font-bold text-amber-900">{error}</p><Link href="/login?next=/drafts" className="mt-4 inline-block rounded-xl bg-blue-700 px-5 py-2.5 text-xs font-black text-white">تسجيل الدخول</Link></div>:items.length===0?<div className="rounded-2xl border bg-white p-12 text-center"><FileText className="mx-auto h-8 w-8 text-slate-400"/><p className="mt-3 font-black text-slate-600">لا توجد مسودات حالية.</p></div>:<div className="grid gap-5 md:grid-cols-2">{items.map(c=><article key={c.id} className="rounded-2xl border border-slate-200 bg-white p-6"><div className="font-mono text-[10px] text-slate-400">{c.serialNumber}</div><h2 className="mt-2 text-lg font-black text-slate-950">{c.title||c.templateNameAr}</h2><p className="mt-1 text-xs text-slate-500">مسودة محفوظة وقابلة للاستكمال</p><div className="mt-4 flex items-center gap-1 text-[11px] text-slate-400"><Clock className="h-3.5 w-3.5"/>{new Date(c.updatedAt).toLocaleString("ar-EG")}</div><div className="mt-5 flex justify-end border-t border-slate-100 pt-4"><Link href={`/wizard/${c.templateSlug}?contractId=${c.id}`} className="inline-flex items-center gap-1 rounded-xl bg-blue-700 px-4 py-2.5 text-xs font-black text-white">استكمال العقد<ArrowLeft className="h-4 w-4"/></Link></div></article>)}</div>}</main><Footer/></div>}
