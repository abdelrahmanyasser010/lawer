{
  "name": "zdraft-workspace",
  "version": "0.4.0",
  "private": true,
  "workspaces": [
    "frontend",
    "dashboard",
    "packages/*"
  ],
  "overrides": {
    "postcss": "8.5.25"
  },
  "scripts": {
    "build:engine": "npm --workspace @zdraft/template-engine run build",
    "test:engine": "npm --workspace @zdraft/template-engine run test",
    "dev:frontend": "npm run build:engine && npm --workspace frontend run dev",
    "dev:dashboard": "npm run build:engine && npm --workspace zdraft-dashboard run dev",
    "build:frontend": "npm run build:engine && npm --workspace frontend run build",
    "build:dashboard": "npm run build:engine && npm --workspace zdraft-dashboard run build",
    "build": "npm run build:engine && npm --workspace frontend run build && npm --workspace zdraft-dashboard run build",
    "dev:backend": "php backend/artisan serve --host=0.0.0.0 --port=8000",
    "db:migrate": "php backend/artisan migrate --seed --force",
    "worker:outbox": "php backend/artisan zdraft:process-outbox --limit=50",
    "worker:documents": "php backend/artisan zdraft:process-documents --limit=3",
    "worker:finalize": "php backend/artisan zdraft:finalize-expired-contracts --limit=50",
    "backend:test": "php backend/artisan test",
    "backend:routes": "php backend/artisan route:list --path=api/v1",
    "verify:laravel": "php backend/tools/run_python.php backend/tools/verify_laravel_final_v20.py",
    "test": "npm run test:engine && npm run verify:laravel"
  }
}
