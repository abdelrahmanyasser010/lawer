# @zdraft/template-engine

Shared, framework-agnostic contract template definitions and resolution/validation logic used by the customer frontend and the law-office dashboard.

The package intentionally contains no authentication, storage, payment, or application-specific UI. Those concerns remain inside each application.
